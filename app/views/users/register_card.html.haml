%main.py-4.mb-5
  .d-flex.justify-content-center
    .container.w-50
      %h3 登録済みのクレジットカード
      %hr/
      %h4= @card[:brand]
      %p
        有効期限: #{"#{ @card[:exp_year] } / #{ @card[:exp_month] }"}
      %p
        カード番号: ************#{@card[:last4]}
      = form_with url: mypage_token_users_path, method: :post do |f|
        - if @card.present?
          %script.payjp-button{"data-key" => "<haml_loud", :src => "https://checkout.pay.jp/", :type => "text/javascript"}
            :cdata
              ENV[&#39;PAYJP_PUBLIC_KEY&#39;]
        - else
          %script.payjp-button{"data-key" => "<haml_loud", :src => "https://checkout.pay.jp/", :type => "text/javascript"}
            :cdata
              ENV[&#39;PAYJP_PUBLIC_KEY&#39;]
